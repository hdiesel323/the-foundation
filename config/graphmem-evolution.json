{
  "evolution": {
    "schedule": "0 3 * * *",
    "description": "Daily at 3 AM UTC — runs Ebbinghaus decay, PageRank, and consolidation",

    "ebbinghaus_forgetting": {
      "enabled": true,
      "description": "Applies Ebbinghaus forgetting curve: retention = e^(-t/S) where S is stability",
      "base_stability_days": 30,
      "reinforcement_bonus": 1.5,
      "minimum_retention": 0.05,
      "retention_thresholds": {
        "strong": 0.7,
        "moderate": 0.3,
        "weak": 0.1,
        "forgotten": 0.05
      }
    },

    "pagerank": {
      "enabled": true,
      "damping_factor": 0.85,
      "iterations": 20,
      "convergence_threshold": 0.0001,
      "edge_weight_scaling": true,
      "description": "Weighted PageRank with relationship weights as edge importance"
    },

    "consolidation": {
      "enabled": true,
      "similarity_threshold": 0.85,
      "merge_strategy": "keep_most_connected",
      "description": "Merges near-duplicate entities based on name similarity and shared relationships",
      "consolidation_rules": [
        {
          "rule": "exact_name_match",
          "description": "Merge entities with identical lowercase names",
          "priority": 1
        },
        {
          "rule": "alias_overlap",
          "description": "Merge entities where one's name appears in another's aliases",
          "priority": 2
        },
        {
          "rule": "high_similarity_cooccurrence",
          "description": "Merge entities frequently co-occurring in same memories",
          "priority": 3,
          "min_cooccurrence": 5
        }
      ]
    },

    "temporal_validity": {
      "enabled": true,
      "description": "Tracks when facts were true/false using valid_from and valid_until timestamps",
      "auto_expire_days": 180,
      "supersede_on_conflict": true,
      "conflict_resolution": "newer_wins"
    },

    "pruning": {
      "enabled": true,
      "min_weight_threshold": 0.01,
      "min_retention_threshold": 0.05,
      "orphan_cleanup": true,
      "description": "Removes edges below weight threshold and isolated entities with no connections"
    }
  }
}
